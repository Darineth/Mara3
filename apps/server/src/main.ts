// @mara/server entry point. Replaced with the real server in Phase 2.
import { PROTOCOL_VERSION } from '@mara/protocol';

function main(): void {
  // eslint-disable-next-line no-console
  console.log(`Mara 3 server scaffold — protocol v${PROTOCOL_VERSION}`);
}

main();

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local bootstrap picker. The chosen server's origin is granted IPC at runtime (see grant_remote_ipc in lib.rs), so no hostnames are hardcoded here.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "allow-get-settings",
    "allow-set-server-url",
    "allow-set-auto-connect",
    "allow-open-app",
    "allow-switch-server",
    "allow-mara-log"
  ]
}
